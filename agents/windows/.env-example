# Windows Agent Configuration
# This file should be placed in: agents/windows/.env

# ========== REQUIRED CONFIGURATION ==========

# Unique identifier for this agent (must match server's DEVICE_TO_AGENTS_JSON)
AGENT_ID=windows-agent

# MQTT broker connection
MQTT_HOST=mosquitto.local
MQTT_PORT=1883

# ========== OPTIONAL: PRE-SHUTDOWN COMMANDS ==========
# Commands to run before shutdown (executed in order)
# Uncomment and customize as needed

# Example: Stop Hyper-V VMs
# PRE_SHUTDOWN_CMD_1=Stop-Service -Name 'vmms' -Force

# Example: Stop Docker containers
# PRE_SHUTDOWN_CMD_2=docker stop $(docker ps -q)

# Example: Stop a specific service
# PRE_SHUTDOWN_CMD_3=Stop-Service -Name 'MyService' -Force

# ========== OPTIONAL: CUSTOM SHUTDOWN COMMANDS ==========

# Custom shutdown command (default: shutdown.exe /s /t 60 /f /c 'EcoFlow Critical Battery Shutdown')
# SHUTDOWN_CMD=shutdown.exe /s /t 60 /f /c 'EcoFlow Critical Battery'

# Custom abort command (default: shutdown.exe /a)
# ABORT_CMD=shutdown.exe /a
