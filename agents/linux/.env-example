# Linux Agent Configuration
# This file should be placed in: agents/linux/.env

# ========== REQUIRED CONFIGURATION ==========

# Unique identifier for this agent (must match server's DEVICE_TO_AGENTS_JSON)
AGENT_ID=linux-agent

# MQTT broker connection
MQTT_HOST=mosquitto.local
MQTT_PORT=1883

# ========== OPTIONAL: PRE-SHUTDOWN COMMANDS ==========
# Commands to run before shutdown (executed in order)
# Uncomment and customize as needed

# Example: Stop Docker containers
# PRE_SHUTDOWN_CMD_1=docker stop $(docker ps -q)

# Example: Stop Kubernetes
# PRE_SHUTDOWN_CMD_2=systemctl stop microk8s

# Example: Stop databases
# PRE_SHUTDOWN_CMD_3=systemctl stop postgresql

# Example: Sync filesystems
# PRE_SHUTDOWN_CMD_4=sync

# ========== OPTIONAL: CUSTOM SHUTDOWN COMMANDS ==========

# Custom shutdown command (default: sudo shutdown -h +1)
# SHUTDOWN_CMD=sudo shutdown -h +1

# Custom abort command (default: sudo shutdown -c)
# ABORT_CMD=sudo shutdown -c
